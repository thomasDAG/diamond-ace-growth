## Packages
framer-motion | Page transitions and scroll-triggered animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility function to efficiently merge Tailwind CSS classes in JS

## Notes
- Single page smooth scroll implementation using native CSS `scroll-behavior: smooth`.
- Framer motion used for entrance animations to give a premium feel.
- Form submits to `POST /api/contact`.
- Custom color palette mapped to requested hex codes in index.css.
